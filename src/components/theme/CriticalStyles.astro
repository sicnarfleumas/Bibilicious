---
// Import the critical CSS file with essential styles
import '../../styles/critical.css';
---

<!-- This component doesn't render anything visible, it just imports the critical CSS -->
<!-- It should be included in all pages to ensure essential styles are loaded --> 