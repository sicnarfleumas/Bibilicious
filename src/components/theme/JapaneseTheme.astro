---
// Import the CSS file with all Japanese transitions and theme styles
import '../../styles/japanese-transitions.css';
---

<!-- This component doesn't render anything visible, it just imports the CSS -->
<!-- It can be included in the main layout to apply the Japanese theme site-wide --> 